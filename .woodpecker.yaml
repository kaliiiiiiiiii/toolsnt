steps:
  test:
    when:
      event: [push, pull_request]
    image: docker.io/rnestler/archlinux-rust
    environment: [CARGO_TERM_COLOR=always]
    commands:
      - pacman -Syu hivex wimlib
      - cargo clippy
      - cargo test
